# =============================================================================
# VIDEOCALL SERVICE - Environment Variables
# =============================================================================
# Copy to .env.development (local) or .env.docker (Docker)
# =============================================================================

# Database
ConnectionStrings__DefaultConnection=Host=localhost;Database=skillswap_videocalls;Username=skillswap;Password=your-db-password;Port=5432;Pooling=true;MinPoolSize=5;MaxPoolSize=50

# Redis (DB 5 for VideocallService - also used for SignalR backplane)
ConnectionStrings__Redis=localhost:6379,connectTimeout=60000,connectRetry=3,defaultDatabase=5

# JWT Settings
JwtSettings__Secret=your-64-char-base64-jwt-secret
JwtSettings__Issuer=SkillSwap
JwtSettings__Audience=SkillSwap

# RabbitMQ
RabbitMQ__Host=localhost
RabbitMQ__Username=guest
RabbitMQ__Password=guest

# M2M Authentication
ServiceCommunication__M2M__ClientSecret=your-m2m-client-secret
