# =============================================================================
# PAYMENT SERVICE - Environment Variables
# =============================================================================
# Copy to .env.development (local) or .env.docker (Docker)
# =============================================================================

# Database
ConnectionStrings__DefaultConnection=Host=localhost;Database=skillswap_payments;Username=skillswap;Password=your-db-password;Port=5432;Pooling=true;MinPoolSize=5;MaxPoolSize=50

# Redis (DB 6 for PaymentService)
ConnectionStrings__Redis=localhost:6379,connectTimeout=60000,connectRetry=3,defaultDatabase=6

# JWT Settings
JwtSettings__Secret=your-64-char-base64-jwt-secret
JwtSettings__Issuer=SkillSwap
JwtSettings__Audience=SkillSwap

# RabbitMQ
RabbitMQ__Host=localhost
RabbitMQ__Username=guest
RabbitMQ__Password=guest

# M2M Authentication
ServiceCommunication__M2M__ClientSecret=your-m2m-client-secret

# =============================================================================
# PAYMENT SERVICE SPECIFIC - Stripe
# =============================================================================

# Stripe API Keys
# Test keys start with sk_test_ / pk_test_
# Live keys start with sk_live_ / pk_live_
Stripe__SecretKey=sk_test_your-stripe-secret-key
Stripe__PublishableKey=pk_test_your-stripe-publishable-key

# Stripe Webhook Secret
# Get from: https://dashboard.stripe.com/webhooks
Stripe__WebhookSecret=whsec_your-webhook-secret
