# =============================================================================
# GATEWAY SERVICE - Environment Variables
# =============================================================================
# Copy to .env.development (local) or .env.docker (Docker)
# =============================================================================

# JWT Settings (for token validation only - Gateway doesn't issue tokens)
JwtSettings__Secret=your-64-char-base64-jwt-secret
JwtSettings__Issuer=SkillSwap
JwtSettings__Audience=SkillSwap

# M2M Authentication
ServiceCommunication__M2M__ClientSecret=your-m2m-client-secret

# Redis (for response caching)
ConnectionStrings__Redis=localhost:6379,connectTimeout=60000,connectRetry=3

# RabbitMQ (for health checks)
RabbitMQ__Host=localhost
RabbitMQ__Username=guest
RabbitMQ__Password=guest
