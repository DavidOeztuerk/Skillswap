# =============================================================================
# USER SERVICE - Environment Variables
# =============================================================================
# Copy to .env.development (local) or .env.docker (Docker)
# =============================================================================

# Database
ConnectionStrings__DefaultConnection=Host=localhost;Database=skillswap_userservice;Username=skillswap;Password=your-db-password;Port=5432;Pooling=true;MinPoolSize=5;MaxPoolSize=50

# Redis (DB 0 for UserService)
ConnectionStrings__Redis=localhost:6379,connectTimeout=60000,connectRetry=3,defaultDatabase=0

# JWT Settings
JwtSettings__Secret=your-64-char-base64-jwt-secret
JwtSettings__Issuer=SkillSwap
JwtSettings__Audience=SkillSwap

# RabbitMQ
RabbitMQ__Host=localhost
RabbitMQ__Username=guest
RabbitMQ__Password=guest

# M2M Authentication
ServiceCommunication__M2M__ClientSecret=your-m2m-client-secret

# Encryption Key (for SecretManager)
SecretManager__EncryptionKey=your-32-char-base64-encryption-key

# =============================================================================
# USER SERVICE SPECIFIC - OAuth
# =============================================================================

# LinkedIn OAuth 2.0
OAuth__LinkedIn__ClientId=your-linkedin-client-id
OAuth__LinkedIn__ClientSecret=your-linkedin-client-secret

# Xing OAuth 1.0a
OAuth__Xing__ConsumerKey=your-xing-consumer-key
OAuth__Xing__ConsumerSecret=your-xing-consumer-secret
